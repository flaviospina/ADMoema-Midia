# Porta do servidor HTTP
PORT=3000
# Caminho do arquivo SQLite (criado automaticamente)
DB_PATH=./data/admoema-midia.db
# Token para acessar o painel /admin e a API de leitura das respostas
ADMIN_TOKEN=troque-este-token
